tasks:
  my_task:
    description: "Fresh news"
    commands:
      - pip install -r requirements.txt
      - python3 main.py
    conda:
      channels:
        - conda-forge
      dependencies:
        - python=3.12.2
    pip:
      dependencies:
        - robotframework
        - robotframework-seleniumlibrary
